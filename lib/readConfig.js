'use strict';

const readConfig = async function (options) {
  if (!options) {
    throw new Error('Options are missing.');
  }
  if (!options.consulUrl)
    if (!options.agent) {

    }
};

module.exports = readConfig;